public class StreamerCMD {
    void handleCMD(Streamer s, StreamsManager ST2, String str){
        String[] words = str.split(" ");
        int Id = Integer.parseInt(words[0]);

        if(words[1].equals("LIST")){
            // System.out.print("[{\"id\":\"" + s.getId() + "\",\"name\":\"" + s.getName() + "\"");
        }

    }

}
